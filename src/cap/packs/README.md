Radio packs = preset knobs only (no capture methods).

Kept:
  STOCK  — built-in defaults (pack index 0, not a .cpp)
  ALL    — pack_ours.cpp   (soft everyday kick)
  PAN    — pack_pan.cpp    (medium / client-oriented knobs)
  FOCUS  — pack_porkchop.cpp (patient lock / depth-oriented knobs)
  LIGHT  — pack_light.cpp  (1.2.5-style light hop)
  CUSTOM — hand-tuned (0xFF in NVS)

Removed: MAX, QUIET, LOUD (aggro/stealth/wolf).
